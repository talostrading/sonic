version: 2.1

jobs:
  build:
    docker:
      - image: cimg/go:1.19
    steps:
      - checkout
      - run:
          name: Build
          command: make
      - run:
          name: Run tests
          command: make test
      - run:
          name: Run benchmarks
          command: make bench
